<%- include('includes/header.ejs') %>
  </head>
  <body class="d-flex flex-column h-100">
    <%- include('includes/navigation.ejs') %>

    <main class="flex-shrink-0">
      <div class="container">
        <div class="row align-items-center g-lg-5 py-5">
          <div class="col-lg-7 text-center text-lg-start">
            <h1 class="display-4 fw-bold lh-1 mb-3">Create your account!</h1>
            <p class="col-lg-10 fs-4">
              This is where your users will register for your site.
            </p>
            <p class="col-lg-10 fs-4">
              An email will be sent to the user for them to verify their email address.
              This is a one time code, which after used to verify the account, will be discarded.
            </p>
            <p class="col-lg-10 fs-4">
              If the user forgets their code or never got the email, they can request a new one be sent on the verification page.  This page, along with the reset password link,
              can be found on the login page.
            </p>
          </div>
          <div class="col-md-10 mx-auto col-lg-5">
            <form
              id="createAccountForm"
              class="p-4 p-md-5 border rounded-3 bg-light"
              action="/createAccount"
              method="POST"
            >
              <% if (errorMessage){ %>

              <span
                id="duplicateEmailError"
                class="custom-invalid"
                ><%= errorMessage %></span
              >

              <% } %>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <div class="form-floating mb-3">
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  name="email"
                  placeholder="name@example.com"
                  required
                />
                <label for="email">Email address</label>
              </div>
              <div class="form-floating mb-3">
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  placeholder="Password"
                  required
                />
                <label for="password">Password</label>
              </div>
              <div class="form-floating mb-3">
                <input
                  type="password"
                  class="form-control"
                  id="confirmPassword"
                  name="confirmPassword"
                  placeholder="Confirm password"
                  required
                />
                <label for="confirmPassword">Confirm Password</label>
                <span
                  hidden
                  id="passwordMismatch"
                  class="custom-invalid"
                  >Passwords do not match.</span
                >
              </div>
              <div class="invalid-feedback">Passwords do not match.</div>
              <button class="w-100 btn btn-lg btn-primary" type="submit">
                Sign up
              </button>
              <hr class="my-4" />
              <small class="text-muted"
                >By clicking Sign up, you agree to the terms of use.</small
              >
            </form>
          </div>
        </div>
      </div>
    </main>

    <%- include('includes/footer.ejs') %>

    <script src="/bootstrap/js/bootstrap.bundle.js"></script>
    <script src="/js/custom.js"></script>
    <%- include('includes/end.ejs') %>